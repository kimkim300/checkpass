# 이용약관·개인정보처리방침 초안 — 확인이 필요한 질문과 근거

작성일 기준으로 코드(`public/js/*.js`, `firestore.rules`, `storage.rules`, `firebase.json`)와 화면(`public/*.html`)을 직접 읽고 확인한 결과입니다. 확인한 사실(코드 근거 있음)과 추정·미확인 사항을 구분했습니다.

## 1. 코드에서 확인한 사실 (질문 아님, 참고용)

| 항목 | 확인 내용 | 근거 |
|---|---|---|
| 회원가입/로그인 계정 | 학생·보호자용 계정 없음. 교사만 숫자 PIN으로 로그인 | `public/teacher.html`, `public/js/auth.js` |
| 수집 항목 | 학생 이름·번호·성별, 결석/체험학습 관련 서술형 내용, 보호자 성명·서명(이미지), 증빙사진(선택) | `public/js/utils.js`(FIELD_TARGETS_BY_TYPE), `public/js/submit.js` |
| 저장 위치 | Firebase Firestore + Storage (Google Cloud) | `public/js/firebase-init.js` |
| Storage 리전 | US-EAST1 (무료 위치) — 이번 대화에서 콘솔 설정 화면으로 직접 확인 | 대화 중 Firebase 콘솔 스크린샷 확인 |
| 접근 제한 | 제출 기록(records)은 해당 학급을 만든 교사 계정만 열람 가능 | `firestore.rules`의 `isOwner(classId)` 조건 |
| 자동 삭제 기능 | 없음. 모두 교사가 수동으로 "파일 정리" 또는 "삭제" 버튼을 눌러야 함 | `public/js/export.js`, `public/js/dashboard.js` 전체 검토 |
| 삭제 버튼의 한계 | 대시보드 개별 "삭제"는 Firestore 기록만 지우고 첨부 파일(Storage)은 남음 | `public/js/dashboard.js` 383~387행 `deleteDoc`만 호출, `deleteObject` 없음 |
| 외부 AI 사용 | 없음 (OpenAI 등 AI API 호출 코드 없음) | `public/js/*.js`, `public/*.html` 전체 검색 결과 없음 |
| 광고·결제 | 없음 | 전체 검색 결과 없음 |
| 제3자 CDN | Google Fonts, jsDelivr, cdnjs, unpkg (글꼴·라이브러리만, 개인정보 전송 없음) | 각 HTML의 `<script>`/`<link>` 태그 |

## 2. 답변 반영 완료

| 번호 | 질문 | 답변 | 반영 내용 |
|---|---|---|---|
| 1 | 학교 정식 명칭 | 표기하지 않음 | 두 문서에서 학교명 관련 문구를 모두 삭제, "담임 교사가 맡은 학급"으로 일반화 |
| 6 | 보유 기간 | 1년 | 제4조·제6조에 "제출일로부터 1년 후 파기"로 명시. 단, 자동 파기 기능이 없어 실제로는 수동 삭제가 필요하다는 점도 함께 적었습니다 |
| 7 | 학교운영위원회 심의 여부 | 아직 안 거침 | 이용약관 제6조에 "아직 공식 절차를 거치지 않았다"고 명시 |
| 8 | 증빙사진(민감정보) 처리 근거 | 법률상 의무로 처리 | 제3조에 개인정보 보호법 제23조제1항제2호 등 근거로 명시, 만 14세 미만 별도 동의 화면도 만들지 않는 것으로 정리 |
| 2 | 담임 교사 성명 표기 여부 | 표기하지 않음 | 제8·9조에서 "실명 비공개"로 명시, 직위(담임 교사)만 표기 |

## 3. 아직 답변이 필요한 항목

| 번호 | 질문 | 왜 필요한가 |
|---|---|---|
| 3 | 실명 없이도 연락 가능한 문의 채널은 무엇으로 하시겠어요? (예: 학급 알림장 애플리케이션, 학교 교무실 대표번호 등) | 실명을 안 밝히더라도 정보주체가 문의·권리 행사를 할 수 있는 창구가 최소 하나는 있어야 합니다. 이게 없으면 제7·8·9조가 실효성이 없어집니다 |
| 4 | 이 방침·약관의 시행일자는 언제로 하시겠어요? | 부칙 |
| 5 | Firestore Database를 만들 때 위치를 어디로 선택하셨나요? (Firebase 콘솔 > Firestore Database > 설정에서 확인 가능) | 국외 이전 고지 정확도 — Storage는 미국(US-EAST1)으로 확인했지만 Firestore는 별도 설정값이라 코드로 확인 불가 |
| 9 | 대시보드 "삭제" 버튼이 첨부 파일까지 완전히 지우도록, 그리고 "1년 지난 기록"을 자동으로 찾아 정리하도록 코드를 보완할까요? | 이번에 정한 1년 보유·완전 파기 원칙을 실제로 지키려면 필요합니다. 원하시면 별도로 구현해드릴게요 |

## 4. 이번에 하지 않은 것

- 이용약관·개인정보처리방침을 앱 화면(예: 푸터)에 실제로 연결하지 않았습니다.
- git add/commit/push, 배포를 하지 않았습니다. 초안 3개 파일만 프로젝트 폴더에 저장했습니다.
  - `개인정보처리방침(초안).md`
  - `이용약관(초안).md`
  - `확인이_필요한_질문.md` (이 파일)
